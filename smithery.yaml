# Smithery configuration file: https://smithery.ai/docs/build/project-config

startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    required:
      - linearApiKey
    properties:
      linearApiKey:
        type: string
        description: Linear Personal API Key for authentication
    description: Configuration for Linear MCP Server
  commandFunction:
    # A JS function that produces the CLI command based on the given config to start the MCP on stdio.
    |-
    (config) => ({ command: 'node', args: ['build/index.js'], env: { LINEAR_API_KEY: config.linearApiKey } })
  exampleConfig:
    linearApiKey: sk_test_linearapikey1234567890
